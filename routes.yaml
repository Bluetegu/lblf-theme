routes:

collections:
  /:
    permalink: /{slug}/
    template: index
    filter: tag:-hash-he

  /he/:
    permalink: /he/{slug}/
    template: index-he
    filter: tag:hash-he

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
